<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SolarRiceRakeApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:fontFamily">@font/intervariablefont</item>
        <item name="fontFamily">@font/intervariablefont</item>
    </style>

    <style name="Theme.SolarRiceRakeApp" parent="Base.Theme.SolarRiceRakeApp" />

    <style name="CircularDeterminateProgressBar">
        <item name="android:indeterminateOnly">false </item>
        <item name="android:progressDrawable">@drawable/circle_progressbar</item>
    </style>
</resources>